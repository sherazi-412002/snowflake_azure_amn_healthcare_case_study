services:
  amn-dev:
    build:
      context: .
      dockerfile: Dockerfile
    image: amn-case-study:dev
    working_dir: /app
    env_file:
      - path: .env
        required: false
    volumes:
      - ./:/app
    command: ["python", "-c", "print('AMN development container is ready')"]
